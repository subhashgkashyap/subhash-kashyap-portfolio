# Build tooling and output - regenerated by `npm install` and `npm run build`.
node_modules
dist
.vite

# Editor / assistant config
.claude
.vscode

# Personal working notes - kept on disk, not published
_docs

# Local environment / OS noise
.DS_Store
Thumbs.db
*.local
.env
.env.local
